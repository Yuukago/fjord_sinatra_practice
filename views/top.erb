<button onclick="location.href='memos/new'">追加</button>

<ul>
  <% @memos_data[:memos].each do |memo| %>
   <li>
      <a href='/memos/<%= memo[:id] %>'><%= h(memo[:title]) %></a>
   </li>
  <% end %>
</ul>
